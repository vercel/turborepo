name: Docker Image CI

on: [push, pull_request]

jobs:

  build:
 
    runs-on: ubuntu-18.04
 
    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: | 
        docker --version
        docker build . --file Dockerfile --tag goreng/golang-cross:${{ github.sha }}

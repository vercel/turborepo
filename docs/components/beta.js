export default function RemoteCachingHeader({ children }) {
  return (
    <span
      aria-label="(Beta)"
      className="ml-2 mb-1 align-middle inline-flex items-center px-2.5 py-0.5 rounded-full tracking-wide text-xs font-medium bg-gradient-to-r from-red-500 to-blue-500 uppercase text-white"
    >
      Beta
    </span>
  );
}

{
  "build": {
    "env": {
      "ENABLE_ROOT_PATH_BUILD_CACHE": "1",
      "FORCE_RUNTIME_TAG": "canary"
    }
  },
  "installCommand": "pnpm i -g pnpm && pnpm -v && pnpm store path && pnpm install"
}

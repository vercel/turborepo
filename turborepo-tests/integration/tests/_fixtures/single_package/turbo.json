{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["somefile.txt"],
  "pipeline": {
    "build": {
      "outputs": ["foo"]
    },
    "test": {
      "outputs": [],
      "dependsOn": ["build"]
    }
  }
}

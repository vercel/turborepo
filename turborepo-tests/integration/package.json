{
  "name": "turborepo-tests-integration",
  "scripts": {
    "test": "python3 -m venv .cram_env && .cram_env/bin/pip3 install --quiet --upgrade pip && .cram_env/bin/pip3 install --quiet prysk==0.15.2 pytest \"prysk[pytest-plugin]\" pytest-xdist && .cram_env/bin/pytest -n auto --prysk-shell=`which bash`",
    "test:serial": "prysk tests",
    "test:interactive": "PRYSK_INTERACTIVE=true prysk tests",
    "clean": "rm -rf tests/**/*.t.err"
  },
  "dependencies": {
    "turborepo-tests-helpers": "workspace:*",
    "@turbo/exe-stub": "workspace:*",
    "prysk": "workspace:*"
  },
  "devDependencies": {
    "cross-env": "7.0.3"
  }
}

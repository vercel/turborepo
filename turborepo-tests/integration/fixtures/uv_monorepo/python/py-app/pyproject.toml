[project]
name = "py-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["py-lib"]

[tool.uv.sources]
py-lib = { workspace = true }

[build-system]
requires = ["uv_build>=0.8,<2"]
build-backend = "uv_build"

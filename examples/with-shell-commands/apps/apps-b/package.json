{
  "name": "app-b",
  "description": "A workspace to use as a model for a final application that the rest of your dependencies are building towards.",
  "scripts": {
    "build": "sleep 3 && mkdir -p dist && echo \"Your application output!\" > dist/app-output.txt && echo \"Application B is built!\"",
    "lint": "sleep 3 && echo \"Linted!\"",
    "type-check": "sleep 3 && echo \"Types checked!\""
  },
  "dependencies": {
    "minimum-reqs": "workspace:*",
    "with-workspace-configuration": "workspace:*"
  },
  "devDependencies": {
    "tooling-config": "workspace:*"
  }
}

{
  "name": "consumer-only-a",
  "description": "A workspace to use as a model for a final, top-level application that the rest of your dependencies are building towards.",
  "scripts": {
    "build": "sleep 5 && mkdir -p dist && echo \"Your application output!\" > dist/app-output.txt && echo \"Your application is built!\""
  },
  "dependencies": {
    "tooling-config": "workspace:*"
  }
}

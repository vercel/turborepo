{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["!README.md"],
      "outputs": ["a-different-file.txt"]
    }
  }
}

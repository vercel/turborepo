{
  "name": "with-workspace-configuration",
  "description": "Uses different a different output file and ignores changes in README.md.",
  "scripts": {
    "build": "sleep 1 && pnpm write-file && pnpm read-file",
    "write-file": "echo \"I used a different output file!\" > a-different-file.txt",
    "read-file": "cat a-different-file.txt",
    "lint": "sleep 1 && echo \"Linted!\"",
    "type-check": "sleep 1 && echo \"Types checked!\""
  },
  "devDependencies": {
    "tooling-config": "workspace:*"
  }
}

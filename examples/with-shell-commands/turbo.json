{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["prebuild", "^build"],
      "outputs": ["output-file.txt", "dist/**"]
    },
    "lint": {},
    "topo": {
      "dependsOn": ["^topo"]
    },
    "type-check": {
      "dependsOn": ["^topo"]
    }
  }
}

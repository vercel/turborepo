import {type ReactNode} from "react";

interface Props {
  children?: ReactNode;
}

export const {{ pascalCase name }} = ({ children }: Props) => {
  return (
    <div>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
};

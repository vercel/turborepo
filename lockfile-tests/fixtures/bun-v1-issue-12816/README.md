# Issue 12816

Reproduction for `turbo prune --docker` producing a Bun lockfile that `bun install --frozen-lockfile` rewrites after pruning `api`.

Source: https://github.com/vercel/turborepo/issues/12816

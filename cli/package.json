{
  "name": "cli",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "make clean",
    "build": "make",
    "build:release": "cargo build -p turbo --profile release-turborepo"
  }
}

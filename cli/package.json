{
  "name": "cli",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "make clean",
    "build": "make",
    "build:go": "make go-turbo",
    "build:rust": "cargo build -p turbo",
    "build:go-copy": "echo $PWD && ls ../target/debug && mv go-turbo ../target/debug/go-turbo",
    "test": "make test-go",
    "format": "make fmt-go",
    "lint": "make lint-go",
    "lint:prettier": "prettier -c . --cache --ignore-path=../.prettierignore"
  },
  "devDependencies": {
    "copy-template-dir": "^1.4.0",
    "faker": "^5.1.0",
    "ngraph.generators": "^19.3.0",
    "shelljs": "^0.8.4"
  }
}

{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["out/**"],
      "dependsOn": ["compile"]
    },


    "add-keys-task": {},
    "add-keys-underlying-task": {},


    "omit-keys-task": {
      "dependsOn": [],
      "inputs": ["src/foo.txt"],
      "outputs": ["out/**"],
      "env": ["SOME_ENV"],
      "outputMode": "new-only"
    },
    "omit-keys-underlying-task": {},


    "compile": {},
    "beforecompile": {},
    "somethingelse": {}
  }
}

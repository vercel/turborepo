{
  "name": "depends-on--add-key",
  "scripts": {
    "build": "echo \"building depends-on--add-key\" > lib/foo.txt && echo \"building depends-on--add-key\" > out/foo.txt",
    "compile": "echo \"compiling in depends-on--add-key\"",
    "beforecompile": "echo \"beforecompiling in depends-on--add-key\""
  },
  "packageManager": "npm@8.9.12"
}

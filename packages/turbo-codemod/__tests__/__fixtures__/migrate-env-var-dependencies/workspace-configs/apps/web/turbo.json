{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      // old
      "dependsOn": ["build", "$ENV_2"],
      // new
      "env": ["ENV_1"]
    }
  }
}

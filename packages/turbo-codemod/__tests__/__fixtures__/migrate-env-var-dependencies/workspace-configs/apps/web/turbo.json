{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      // old
      "dependsOn": ["build", "$ENV_2"],
      // new
      "env": ["ENV_1"]
    }
  }
}

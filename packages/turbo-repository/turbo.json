{
  "$schema": "../../docs/public/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@turbo/cli#rust-src"],
      "env": ["NODE_VERSION", "CARGO_BUILD_JOBS", "RUSTFLAGS"],
      "outputs": ["js/dist/**/*", "native/**/*"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}

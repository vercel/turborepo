{
  "$schema": "../../docs/public/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["cli#rust-src"],
      "env": ["NODE_VERSION"],
      "outputs": ["js/dist/**/*", "native/**/*"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}

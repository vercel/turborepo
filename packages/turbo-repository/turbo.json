{
  "$schema": "../../docs/public/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["cli#rust-src"],
      "env": ["NODE_VERSION", "CARGO_BUILD_TARGET"]
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}

{
  "$schema": "../../docs/public/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "command": ["pnpm", "exec", "jest"]
    },
    "lint:prettier": {
      "command": [
        "pnpm",
        "exec",
        "prettier",
        "-c",
        ".",
        "--cache",
        "--ignore-path=../../.prettierignore"
      ]
    }
  }
}

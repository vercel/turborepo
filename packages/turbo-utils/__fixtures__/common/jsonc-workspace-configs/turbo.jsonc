{
  // Root turbo.jsonc file
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["CI"],
  "tasks": {
    "build": {
      // Environment variables for build
      "env": ["ENV_1"]
    }
  }
}

{
  "$schema": "../../docs/public/schema.json",
  "extends": ["//"],
  "tasks": {
    "check-types": {
      "command": ["pnpm", "exec", "tsc", "--noEmit"]
    },
    "lint:prettier": {
      "command": [
        "pnpm",
        "exec",
        "prettier",
        "-c",
        ".",
        "--cache",
        "--ignore-path=../../.prettier-ignore"
      ]
    }
  }
}

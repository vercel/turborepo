export const DAILY_PERFORMANCE_IMPROVEMENT_PROMPT = `Call \`begin_performance_improvement\` first and load the \`performance_improvement\` skill. Find one focused, measurable Turborepo performance improvement. Prioritize end-to-end improvements to real user workflows over isolated microbenchmark wins; use microbenchmarks to investigate and corroborate hot-path mechanisms, not as a substitute for representative real-world evidence. Follow the skill's scientific method: preserve optimized baseline and candidate binaries, use repeated measurements under controlled conditions, use the pinned open-source corpus when appropriate, corroborate timing mechanisms with a targeted benchmark, and select relevant diagnostics from its performance toolbox. Use \`run_performance_command\` to record the clean-checkout \`baseline\`, same-command \`after\`, paired AB/BA \`comparison\` evidence, and relevant correctness \`validation\` after the final edit.

Inspect the complete diff and send it, every exact benchmark and profiler command, raw measurement outputs, environment details, corpus revisions, and validation results to the reviewer named by \`begin_performance_improvement\`. Request structured output with this schema: { approved: boolean, blockingFindings: string[], summary: string }. Resolve every blocking finding, rerun the after measurement, comparisons, and validation after any edit, and obtain a fresh review. Then call \`record_performance_review\` with the reviewer's verdict.

Only after the exact final diff has successful measurements, validation, and opposite-model approval, call \`create_pull_request\` with a concise Conventional Commit performance title and a body containing the author model, reviewer model, methodology, before/after evidence, and review summary. Do not list routine CI-covered correctness checks; mention validation only when the change required non-routine manual testing beyond the performance measurements and test suite. If no defensible improvement is found, finish without creating a pull request.`;

export const DAILY_PERFORMANCE_IMPROVEMENT_PROMPT = `Call \`begin_performance_improvement\` first and load the \`performance_improvement\` skill. Find one focused, measurable Turborepo performance improvement. Use \`run_performance_command\` to record an unchanged-checkout baseline before editing, the exact same command after editing, and relevant correctness validation after the final edit.

Inspect the complete diff and send it, the exact benchmark command, both outputs, and validation results to the reviewer named by \`begin_performance_improvement\`. Request structured output with this schema: { approved: boolean, blockingFindings: string[], summary: string }. Resolve every blocking finding, rerun the after measurement and validation after any edit, and obtain a fresh review. Then call \`record_performance_review\` with the reviewer's verdict.

Only after the exact final diff has successful measurements, validation, and opposite-model approval, call \`create_pull_request\` with a concise Conventional Commit performance title and a body containing the author model, reviewer model, methodology, before/after evidence, correctness checks, and review summary. If no defensible improvement is found, finish without creating a pull request.`;

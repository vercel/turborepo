export const DAILY_EXAMPLE_MAINTENANCE_PROMPT = `Call \`select_daily_example\` first, then pass the selected example to \`run_opencode_maintenance\`. OpenCode uses GPT 5.6 Sol to maintain and validate only that example.

After OpenCode finishes, inspect its changes and validation summary. When changes exist, call \`create_pull_request\` to open a draft pull request against \`vercel/turborepo\`. Summarize the selected example's changes and validation results in the body. The tool supplies the example-specific branch, Conventional Commit title, and commit message. If there are no changes, finish without creating a pull request.`;
